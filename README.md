# 🌟 BestDay Bot — Ваш личный помощник по продуктивности

**BestDay Bot** — это Telegram-бот, который помогает вам:
- Управлять ежедневными задачами (`/todo`)
- Вести карту судьбоносных желаний (`/wishlist`)
- Отслеживать статистику продуктивности (`/stats`)
- Настраивать личный профиль (`/setcity`)

Бот работает **автоматически**: задачи удаляются каждую ночь, желания — через 2 месяца, а статистика сохраняется ежедневно.

---

## 📌 Основные команды

| Команда | Описание |
|--------|---------|
| `/todo` | Управление ежедневными задачами |
| `/wishlist` | Управление картой желаний |
| `/stats` | Просмотр статистики продуктивности |
| `/setcity` | Установка города для будущих функций |
| `/about` | Информация о боте |
| `/authors` | Авторы проекта |
| `/help` | Справка по всем командам |

> 💡 Подробную справку по любой команде можно получить через:  
> **`/help <команда>`**  
> Например: `/help todo`, `/help wishlist`

---

## 📋 Подробности по функционалу

### 📌 `/todo` — Ежедневные задачи

**Что умеет:**
- ✅ Добавлять задачи: `/todo add Сходить в магазин`
- ✅ Завершать задачи: `/todo complete 2`
- ✅ Редактировать задачи: `/todo edit 1` (с проверкой длины: мин. 2, макс. 50 символов)
- 📊 Автоматически считает процент выполнения
- 🧹 **Автоматически удаляется каждую ночь в 00:00 (Екб)**
- 🧹 При запуске бота удаляет задачи предыдущих дней

> ⚠️ В будущем будет реализовано:  
> 🔔 Напоминания за 1 час и за 5 минут до удаления задач

---

### 🌠 `/wishlist` — Карта желаний

**Философия:**
> «Каждое желание — это диалог со Вселенной. Изменение = отречение от изначального импульса души.»

**Что умеет:**
- ✨ Добавлять желания: `/wishlist add Найти своё предназначение`
- 🔒 Завершить добавление: `/wishlist endadd` → желания **блокируются на 2 месяца**
- ✅ Отмечать как выполненные: `/wishlist complete 3`
- 📅 **Автоматическое удаление через 60 дней** после блокировки

> ❌ Редактирование и удаление желаний **запрещены** — только добавление и завершение  
> ⚠️ В будущем: напоминания перед удалением

---

### 📈 `/stats` — Статистика продуктивности

**Что показывает:**
- 📊 Прогресс за сегодня (`/stats`)
- 📅 Среднюю продуктивность за неделю (`/stats week`)
- 📋 Детальную статистику по дням недели
- 💬 Мотивационные сообщения в зависимости от результата

> ℹ️ Город больше **не отображается** в статистике (только в профиле)

---

### 🌍 `/setcity` — Установка города

- Устанавливает город в вашем профиле
- Город отображается при просмотре желаний и задач
- В будущем будет использоваться для погоды и геозависимых функций

> 🔜 Планируется: переделать в подкоманду `/weather get`

---

## 🧪 Тестирование

Проект покрыт **автоматическими тестами**:

| Компонент | Тип тестов | Статус |
|----------|-----------|--------|
| `/todo` | ✅ Юнит-тесты (Mockito) <br> ✅ Интеграционные тесты (Testcontainers + PostgreSQL) | Готово |
| `/wishlist` | ✅ Юнит-тесты (Mockito) <br> ✅ Интеграционные тесты (Testcontainers) | Готово |
| База данных | ✅ Интеграционные тесты всех операций | Готово |

Запуск тестов:
```bash
# Gradle
./gradlew test

# Maven
mvn test
```

---

## 🛠 Архитектура

- **Расширяемая система команд**: при добавлении новой команды она автоматически появляется в `/help`
- **Чистое разделение слоёв**:  
  `Command` → `DatabaseManager` → PostgreSQL
- **Автоматическая очистка**: фоновые задачи управляют жизненным циклом данных
- **Безопасность**: все SQL-запросы используют `PreparedStatement`

---

## 🚀 Запуск

1. Установите **Java 17+** и **Docker** (для тестов)
2. Настройте переменные окружения:
   ```env
   BOT_TOKEN=your_telegram_bot_token
   BOT_USERNAME=your_bot_username
   DATABASE_URL=jdbc:postgresql://localhost:5432/bestday
   DATABASE_USER=postgres
   DATABASE_PASSWORD=postgres
   ```
3. Соберите и запустите:
   ```bash
   ./gradlew bootRun
   ```

---

## 📜 Лицензия

MIT © 2025 BestDay Bot Team

---

> 💫 **BestDay Bot — не просто список задач, а путь к осознанной продуктивности.**  
> Каждое выполненное желание — шаг к новой версии себя.


Вот структурированное описание **второй и третьей задач** проекта **BestDay Bot**, оформленное как техническое ТЗ:

---

## 📌 **Задача 2: Реализация ядра бота — задачи, желания, город и статистика**

### 🎯 Цель
Создать систему управления **ежедневными задачами**, **долгосрочной картой желаний**, **персональным городом** и **сохранением статистики** в базе данных.

---

### ✅ Функциональные требования

#### 1. **Ежедневные задачи (`/todo`)**
- Пользователь может добавлять задачи на текущий день.
- Задачи **автоматически удаляются через ~24 часа** (в 23:59 по Екб).
- Каждая задача имеет:
    - Текст (2–50 символов)
    - Статус: завершена / не завершена
    - Время создания
- Поддержка команд:
    - `/todo add <текст>`
    - `/todo complete <номер>`
    - `/todo edit <номер>` — с валидацией длины
    - `/todo` — просмотр всех задач

#### 2. **Карта желаний (`/wishlist`)**
- Пользователь может добавлять **долгосрочные желания**.
- Желания **хранятся 60 дней**, затем удаляются.
- Возможность **отметить желание как выполненное**, но **нельзя редактировать или удалять**.
- Поддержка фазы «фокусировки»:
    - `/wishlist endadd` — завершает добавление, блокирует новые желания на 60 дней.
- Поддержка команд:
    - `/wishlist add <текст>`
    - `/wishlist complete <номер>`
    - `/wishlist endadd` - блокировка
    - `/wishlist` — просмотр

#### 3. **Город пользователя (`/setcity`)**
- Пользователь может указать **свой город**.
- Город сохраняется в БД и используется:
    - Для персонализации интерфейса
    - В будущем — для прогноза погоды

#### 4. **Статистика продуктивности**
- При добавлении/завершении задачи **автоматически пересчитывается процент выполнения** за день:
  ```
  completion_rate = (выполнено / всего задач) * 100
  ```
- Данные сохраняются в таблице `productivity_stats`:
    - `user_id`
    - `completion_rate`
    - `stat_date`
    - `total_tasks`, `completed_tasks`
- Статистика используется в `/stats` для отображения:
    - Сегодняшнего прогресса
    - Недельной динамики

#### 5. **Подключение к PostgreSQL**
- Использовать **connection pooling** (например, через `DriverManager` или HikariCP).
- Все операции — через **параметризованные запросы** (защита от SQL-инъекций).
- Таблицы:
    - `users` (user_id, city)
    - `daily_tasks`
    - `wishlist`
    - `productivity_stats`

---

## 📌 **Задача 3: Ежедневная утренняя рассылка с погодой и Таро**

### 🎯 Цель
Ежедневно в **утреннее время (например, 08:00 по Екб)** отправлять пользователю персонализированное сообщение, включающее:
1. Прогноз погоды для его города
2. Случайную карту Таро дня + толкование
3. Призыв заполнить `/todo` на день

---

### ✅ Функциональные требования

#### 1. **Парсинг погоды**
- Использовать публичный API (например, OpenWeatherMap, WeatherAPI).
- Запрос по **городу из профиля пользователя**.
- Кэшировать данные на 1 час, чтобы не превысить лимиты API.
- Обрабатывать ошибки (город не найден, нет интернета).

#### 2. **Генерация карты Таро дня**
- Иметь локальный набор из **78 карт Таро** (например, в JSON или БД).
- Ежедневно выбирать **одну случайную карту**.
- Парсить или хранить **толкование карты** (значение, совет, предупреждение).
- Формат сообщения:
  ```
  🌞 Доброе утро!
  
  🌤 Погода в Екатеринбурге: +12°C, ☁️ пасмурно
  
  🃏 Карта дня: Сила
  💬 "Сила — это внутренняя уверенность и умение управлять своими эмоциями..."
  
  ✨ Начни день с намерения — добавь задачи: /todo add ...
  ```

#### 3. **Рассылка**
- Запускать **ежедневно в 08:00 по Екб** через `ScheduledExecutorService`.
- Отправлять **только активным пользователям** (у кого есть город или задачи за последние 7 дней).
- Обрабатывать ошибки отправки (пользователь заблокировал бота).


## 🧪 Тестирование (обязательно!)

- Unit-тесты для логики парсинга погоды и Таро
- Интеграционные тесты с моками API
- Тесты расписания (через Mockito + `ScheduledExecutorService`)
